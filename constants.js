import ROUTES from './routes.constants';

const CONSTANTS = {
  NAVBAR: {
    MENU_DATA: [
      {
        link: ROUTES.HOME,
        title: 'Home'
      },
      {
        link: ROUTES.EXPENSES,
        title: 'Expenses'
      }
    ]
  }
};
export default CONSTANTS;
