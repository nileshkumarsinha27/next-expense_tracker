/**
 * Importing the dependencies
 */
import Head from 'next/head';

/**
 *Head component to be used throughout the application
 */
const HeadComponent = () => (
  <Head>
    <title>Expense Tracker</title>
    <link rel="icon" href="/favicon.svg" />
  </Head>
);
export default HeadComponent;
